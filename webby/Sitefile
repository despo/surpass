task :screenshots do
  require "tasks/screenshots"
end

task :default => :pdf

desc "Move generated manual to surpass root directory."
task :copy do
  `cp output/manual.pdf ..`
end

task :all => [:clobber, :screenshots, :pdf, :copy]
